<h2> <%= @user.username %>'s Masterpieces </h2>

<% @pictures.each do |picture| %>

<div class="picture-display">
  <img src = <%= picture.image%> alt="a picture" width="200px" height="200px"  />
<div id="zoom">
  <%= link_to "Zoom", show_image_path("#{picture.id}") %>
</div>
</div>

<% end %>
